---
owner: grafana
git-repo: helm-charts
skip-existing: true
release-name-template: "{{ .Name }}-{{ .Version }}"
# Configure tag format without 'v' prefix for cloudcost-exporter
git-tag-template: "{{ .Name }}-{{ .Version }}"
